using UnityEditor;

namespace Hierarchy.Data {
    public class HierarchyIconData : ScriptableSingleton<HierarchyIconData>{
           
        //To create editor icons that automatically adapt to Unity's dark mode,
        //you can use a simple naming convention: prefix the icon name with "d_"
        //for the dark mode version, and the standard icon name for the light mode version. 
    }
}